<resources>

    <!-- Application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">

    </style>

    <style name="AppThemeActionBar" parent="@style/Theme.AppCompat.Light.DarkActionBar">

        <item name="android:actionBarStyle">@style/ContactsMaterialActionBar</item>
        <item name="android:textSize">@dimen/text_size</item>
        <!-- Support library compatibility -->
        <!--<item name="actionBarStyle">@style/ContactsMaterialActionBar</item>-->

    </style>

    <!-- ActionBar styles -->
    <style name="ContactsMaterialActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/primary</item>
        <item name="android:textSize">@dimen/text_size</item>

        <!-- Support library compatibility -->
        <item name="background">@color/primary</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>

</resources>
